# Caleyez — Repository State Log

> **Purpose:** Every time you push new files, models, or scripts to GitHub,
> record the change here. This file is committed and pushed so the next
> session (human or AI) knows exactly what is already on the remote and what
> is new.

---

## Initial Push — 2026-05-31

### Python Scripts committed
| File | Purpose |
|------|---------|
| `train_general_model.py` | Trains the Global YOLO11l-cls food classifier |
| `train_israeli_food.py` | Trains the Israeli YOLO11l-cls food classifier |
| `generate_arbiter_dataset.py` | Runs inference on val/test splits → produces CSV for XGBoost |
| `generate_yaml.py` | Generates `data.yaml` for dataset configuration |

### Configuration / Data files committed
| File | Purpose |
|------|---------|
| `data.yaml` | YOLO dataset config (class names, paths) |

### Documentation committed
| File | Purpose |
|------|---------|
| `docs_general_model.html` | HTML documentation for the Global model |
| `inference_mechanics_explained.html` | Deep-dive: YOLO inference mechanics for the Arbiter |
| `GENERAL.md` | General project notes |
| `REPO_STATE.md` | This file — repo state log |

### Model Run Artifacts committed
#### `runs/general_model_yolo11l5/` — Global Model (Final Production Run)
- `args.yaml` — Training hyperparameters
- `results.csv` — Per-epoch training metrics
- `results.png` — Training curves
- `confusion_matrix.png` / `confusion_matrix_normalized.png`
- `train_batch0-2.jpg`, `val_batch0-2_labels.jpg`, `val_batch0-2_pred.jpg`
- `weights/best.pt` — **THE production Global model weights** ✅

#### `runs/israeli_food_yolo11l/` — Israeli Model (Final Production Run)
- `args.yaml` — Training hyperparameters
- `results.csv` — Per-epoch training metrics
- `results.png` — Training curves
- `confusion_matrix.png` / `confusion_matrix_normalized.png`
- `train_batch0-2.jpg`, `val_batch0-2_labels.jpg`, `val_batch0-2_pred.jpg`
- `weights/best.pt` — **THE production Israeli model weights** ✅

### Explicitly EXCLUDED from repo (see `.gitignore`)
| Excluded Item | Reason |
|---------------|--------|
| `datasets/` | Massive image datasets (~GBs). Too large for GitHub. |
| `*.pt` (epoch checkpoints + root weights) | Large binary files. Only `best.pt` of each production run is kept. |
| `runs/general_model_yolo11l/` | Aborted/intermediate run — empty weights. |
| `runs/general_model_yolo11l2/` | Aborted/intermediate run — empty weights. |
| `runs/general_model_yolo11l3/` | Aborted/intermediate run — empty weights. |
| `runs/general_model_yolo11l4/` | Aborted/intermediate run — empty weights. |
| `arbiter_training_dataset.csv` | Large raw inference output. Re-generated by `generate_arbiter_dataset.py`. |
| `.idea/` | PyCharm IDE files. |
| `.claude/` | Claude Code internal settings. |

---

## How to update this log

When you add new files or train new models, **append a new section** below using this template:

```markdown
## Push — YYYY-MM-DD — <short description>

### New / Changed Files
| File | Change | Notes |
|------|--------|-------|
| `new_script.py` | Added | Describe what it does |
| `runs/new_run/weights/best.pt` | Added | Model X, accuracy Y% |

### Updated .gitignore rules (if any)
- Added exclusion for `...`
- Un-excluded `...`
```

---

## Model Registry

| Model Name | Run Folder | Weights Path | Dataset | Notes |
|-----------|-----------|-------------|---------|-------|
| Global YOLO11l-cls | `runs/general_model_yolo11l5` | `runs/general_model_yolo11l5/weights/best.pt` | `datasets/general_model_data_set` | Production Global model |
| Israeli YOLO11l-cls | `runs/israeli_food_yolo11l` | `runs/israeli_food_yolo11l/weights/best.pt` | `datasets/israeli-food-master` | Production Israeli model |
| XGBoost Arbiter | _not yet trained_ | — | `arbiter_training_dataset.csv` | Router meta-learner — next step |
